# Questions de spécification (à trancher par l'instance chat qui détient la matrice LEON)

Procédure (CLAUDE.md §6) : en cas d'ambiguïté métier, NE PAS deviner. Consigner ici, Bastien remonte à l'instance chat qui tranche avec les cellules sources sous les yeux, puis on met à jour le dictionnaire et on implémente.

| # | Règle | Question | Statut | Réponse / cellule source |
|---|---|---|---|---|
| Q-10 | R-LOYER-4 | Borne intermédiaire du ratio LCR (entre 10 % et 20 %) : le moteur applique `ratio/100` conformément au dictionnaire, mais la formule source `calculs!B152:B153` n'a pas encore été relue caractère par caractère (le bloc est vide sur l'opération PLS Bergerac, qui n'a pas de LCR). À confirmer sur une opération qui en comporte. | ouverte | — |
| Q-11 | R-EXP | Le compte d'exploitation implémente la mécanique du dictionnaire (assiettes, indexation par trajectoire, enchaînement des soldes) mais **n'est confronté à aucun golden test** : les colonnes de LEON (`SimPLS` DO..EC et cachées GU/HB/HH/HX/IA) n'ont pas été mappées ligne à ligne. Points à trancher en priorité : (a) assiette exacte des produits financiers (autofinancement cumulé ? trésorerie ? fonds propres ?), (b) les frais de gestion sont-ils un forfait par logement indexé ou un % des loyers selon le produit, (c) traitement du REL et de la PGE/PGERC. | ouverte | — |
| Q-12 | R-FIN-4 | Le « redressement » et l'option PLUS Horizen (prêts > 49 ans affectés PLUS → basculés en foncier) sont cités au dictionnaire mais non implémentés : aucune opération disponible ne les exerce. Confirmer qu'ils sont hors périmètre V1. | ouverte | — |
| Q-1 | R-AMT-4 | Dernière échéance ajustée (SimPLUS!FK117). | **close** 04/08/2026 | Pas de cas particulier dans LEON : à la dernière année m_N = 1, le facteur d'annuité vaut (1+tx_N), donc l'annuité solde exactement CRD + intérêts. Émergent de la formule. Source `SimPLUS!FK117` (classeur BERGERAC). |
| Q-2 | R-AMT-4 | Révisabilité SIMPLE : le taux suit-il le LA ? | **close** 04/08/2026 | Oui. `SimPLUS!FI117` teste DOUBLE puis D. LIMITEE, tout autre libellé (dont SIMPLE) → progressivité p seule pour l'annuité ; le taux d'intérêt `FJ117`, lui, est révisé sans test. Seul `TAUX FIXE` fige le taux, et uniquement dans le bloc LIBRE (`SimLIB!FH8`) — cf. écart E-3. |
| Q-3 | R-FIN-6 | Capitalisation des tirages de préfinancement. | **close** 04/08/2026 | Actuarielle en base exact/365 : `montant × (1+taux)^((date_fin − date_tirage)/365)`, avec `date_fin` = date du **dernier tirage** (`FA14 = $AL$35`), pas la mise en location. Échéancier = 13 tirages datés (`AL23:AL35`). Source `SimPLUS!FA15:FD27`. |
| Q-4 | R-AMT-3 | « année(DAT) + 1, +0 si démembrement ». | ouverte | Confirmé que chaque prêt porte sa propre date (`AM17`/`AN17`/`AO17`/`AP17`/`AR17`, toutes à 47119 = 01/01/2029 dans le classeur Bergerac, pour une fin de préfi au 01/01/2028). Le **décalage de démembrement** reste à confirmer : la règle est appliquée en amont de SimPLUS, la cellule ne porte qu'une date déjà calculée. Où est-elle calculée (ParaPLUS ? calculs ?) ? |
| Q-5 | R-AMT-4 | Première annuité quand le LA a déjà bougé l'année 1. | **close** 04/08/2026 | Sans objet : LEON ne calcule pas d'« annuité 1 » propagée ensuite. Chaque année est ré-amortie sur le CRD et la durée restante, l'année 1 comprise, aux taux révisés de l'année. La cellule `AM15` n'est qu'un affichage non consommé par le tableau (écart E-1). |
| Q-6 | R-AMT-4 | **Différé de type 1** : LEON enregistre intérêts = 0 et CRD constant (`FL117`/`FM117`/`FN117`), donc les intérêts du différé ne sont ni appelés ni capitalisés — le différé est économiquement gratuit, et le prêt amortit ensuite le capital d'origine sur (durée − différé). Le dictionnaire v0.1 annonçait « intérêts capitalisés ». Bug de LEON ou capitalisation faite en amont dans le montant du prêt (`AL12` = « MONTANT (y/c Préfi) ») ? Le moteur reproduit LEON pour l'instant (écart E-2). | ouverte | — |
| Q-7 | R-AMT-4 | **Source de la trajectoire du Livret A** : l'amortissement lit `ParaGEN!CT22:DD102` (colonne 11), table qui court de 2028 à 2108 et vaut 2 % à plat dans le classeur Bergerac. Le référentiel `trajectoires_axentia_2026.json` du repo vient, lui, de `ParaGEN!GS22:HE72` (2023-2073, LA variable). Deux tables distinctes : laquelle fait foi pour l'amortissement, et faut-il verser CT22:DD102 dans les référentiels ? | ouverte | — |
| Q-8 | R-AMT-1 | Le « Taux référence LA » (`ParaGEN!DD20` = 2,4 %, plage nommée `Tx_LA`) est supérieur à la trajectoire (2 % dès 2028) : tous les prêts démarrent donc avec une révision de **−0,4 point** dès la première échéance. Vérifié sur Bergerac : PLS saisi à 3,51 %, amorti à 3,11 %. Est-ce voulu (LA figé à la date de montage du barème) ou un résidu de paramétrage ? Impact direct sur toutes les annuités. | ouverte | — |
| Q-9 | R-AMT-5 | Bergerac : le prêt « Collecteur 1 » (20 000 EUR à 0,25 %, 40 ans, 1re échéance 2028) est bien saisi dans `SimPLS!AQ12:AQ18` et référencé 673 fois dans le classeur, mais **aucun bloc d'amortissement de `SimPLS` ne le pilote** (les 4 blocs existants portent sur `$AM`, `$AN`, `$AS`, `$AU`). Ses annuités sont-elles calculées ailleurs, ou ce prêt est-il absent du compte d'exploitation ? | ouverte | — |
